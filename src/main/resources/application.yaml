spring:
  application:
    name: ratelimit
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2000ms

management:
  endpoints:
    web:
      exposure:
        include: prometheus,health,info
  metrics:
    export:
      prometheus:
        enabled: true

logging:
  level:
    com.demo.ratelimit: DEBUG
